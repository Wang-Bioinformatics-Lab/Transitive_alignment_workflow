name: Transitive_alignment_distributed_workflow
displayname: STUFF
Views:
-   name: Transitive_alignment_distributed_workflow
    displayname: Transitive_alignment_distributed_workflow
    viewname: Transitive_alignment_distributed_workflow
    displaytype: datatable
    parameters:
        filename: output/summary.tsv
        columns:
            -   title: "View LCMS"
                data: filename
            -   title: "filename"
                data: filename
        columnDefs: '[ {"targets": 0,"data": null,"render": function ( data, type, row, meta ) {
            return `
            <a target="_blank" href="/dashboards/lcms?usi=mzspec:GNPS2-${task}:${encodeURIComponent(row["filename"])}">View LCMS - mzspec:GNPS2-${task}:${row["filename"]}</a>
            `;}}]'
            
